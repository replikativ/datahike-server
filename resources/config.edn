{:databases [{:store {:backend :file
                      :path "/tmp/dh-sessions"}
              :schema-flexibility :read
              :keep-history? false
              :name "sessions"}
             {:store {:backend :file
                      :path "/tmp/dh-users"}
              :name "users"
              :keep-history? true
              :schema-flexibility :write}]
 :server {:port  3000
          :join? false}}
